---
import { Image } from 'astro:assets';
import logo from '../assets/logo.svg';
interface Props {
    variant?: 'header' | 'footer' | 'dark';
    Height: String;
    Width: String;
}

const { variant = 'header',Width,Height } = Astro.props;

// Determine the appropriate CSS class based on the variant
const getLogoClass = () => {
    switch (variant) {
        case 'header':
            return 'text-header-logo hover:text-primary';
        case 'footer':
            return 'text-footer-logo hover:text-primary';
        case 'dark':
            return 'text-body-base hover:text-primary';
        default:
            return 'text-body-base hover:text-primary';
    }
};
---
<a
  href="/"
  class:list={[
    'logo uppercase font-logo transition-colors duration-200 font-light',
    'flex items-center  flex-row-reverse', // reverse image to the right
    getLogoClass()
  ]}
> 
  <Image src={logo} alt="SmartCrowds" height={Height} width={Width} />

</a>

<!-- color 1 : #E2E0DF  color 2 #D0C2A9 coloe 3 #5C331C color 4 #32160B -->
 <!-- #746d6d -->




 <!--  -->